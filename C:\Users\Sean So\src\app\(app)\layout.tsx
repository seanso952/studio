
// This layout file is intentionally empty.
// The root layout's AppShell now handles all authenticated routes.
export default function AppLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
